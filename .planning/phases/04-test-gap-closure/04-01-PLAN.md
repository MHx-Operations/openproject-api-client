---
phase: 04-test-gap-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-test-gap-closure/AUDIT.md
  - tests/test_apiclient.py
  - tests/test_resources.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07, TEST-08]
tags: [testing, audit, coverage, traceability]

must_haves:
  truths:
    - "Each of TEST-01..TEST-08 maps to at least one named test function in tests/test_resources.py or tests/test_apiclient.py with a file:line reference."
    - "The audit table is recorded in a durable artifact (AUDIT.md) inside the phase directory so future readers can re-trace coverage without re-running the audit."
    - "Any TEST-XX flagged as GAP during audit has a focused new test added that covers exactly the missing behavior; no duplicate tests are added for COVERED items."
    - "The full test suite (pytest -v -x) exits 0 on the project's supported Python (3.9–3.13)."
    - "pyproject.toml runtime dependencies are unchanged — no coverage tooling, no new test framework, no linter introduced."
  artifacts:
    - path: ".planning/phases/04-test-gap-closure/AUDIT.md"
      provides: "TEST-01..08 → test-function-name + file:line traceability table with COVERED/GAP disposition per row"
      contains: "TEST-01"
    - path: "tests/test_resources.py"
      provides: "Existing Collection/href tests (already covers TEST-01, TEST-02, TEST-03 via decode); may receive additional tests if audit finds a gap"
    - path: "tests/test_apiclient.py"
      provides: "Existing pagesize-none/unknown-status/debug-log/session-reuse/security-kwargs tests; may receive additional tests if audit finds a gap"
  key_links:
    - from: ".planning/phases/04-test-gap-closure/AUDIT.md"
      to: "tests/test_resources.py and tests/test_apiclient.py"
      via: "file:line citations next to each TEST-XX row"
      pattern: "tests/test_(apiclient|resources)\\.py:[0-9]+"
    - from: "TEST-XX rows in AUDIT.md"
      to: "REQUIREMENTS.md TEST section"
      via: "verbatim TEST-XX identifier"
      pattern: "TEST-0[1-8]"
---

<objective>
Close the test coverage gaps catalogued in `.planning/codebase/CONCERNS.md` for TEST-01..TEST-08 by:
1. AUDITING the existing test suite (built up incidentally during Phases 1–3) and producing a `TEST-XX → test_function_name (file:line)` traceability table.
2. Adding focused tests only where the audit reveals a genuine gap.
3. Confirming the full suite remains green.

Purpose: Most TEST-XX requirements are already satisfied by tests added during the TDD work of Phases 1–3. This plan ensures that fact is documented (so it survives future refactors) and that any actual blind spot is closed without duplicating existing coverage. The plan is deliberately audit-first to avoid the trap of writing eight redundant new test classes.

Output:
- `.planning/phases/04-test-gap-closure/AUDIT.md` — the traceability table, durable, committed.
- Zero or more new test functions in `tests/test_resources.py` / `tests/test_apiclient.py` for genuine gaps.
- Full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-latent-bug-fixes/01-01-SUMMARY.md
@.planning/phases/01-latent-bug-fixes/01-02-SUMMARY.md
@.planning/phases/01-latent-bug-fixes/01-03-SUMMARY.md
@.planning/phases/02-security-baseline/02-01-SUMMARY.md
@.planning/phases/02-security-baseline/02-02-SUMMARY.md
@.planning/phases/03-performance-baseline/03-01-SUMMARY.md
@.planning/phases/03-performance-baseline/03-02-SUMMARY.md
@tests/test_apiclient.py
@tests/test_resources.py
@tests/conftest.py

<known_existing_coverage>
<!--
Pre-audit inventory extracted from Phase 1–3 SUMMARYs and a grep of the test files.
The executor MUST verify each citation by running grep against the current tree
before marking a row COVERED in AUDIT.md. This block is the executor's starting
hypothesis, not a substitute for the audit itself.

  TEST-01  Collection missing _embedded / .elements
    - tests/test_resources.py: TestCollection.test_collection_missing_embedded         (line ~630)
    - tests/test_resources.py: TestCollection.test_collection_embedded_without_elements (line ~636)
    - tests/test_resources.py: TestCollection.test_collection_empty_elements_regression (line ~641)
    - tests/test_resources.py: TestCollection.test_collection_items_default_is_list     (line ~646)

  TEST-02  href parsing — None / empty / URN
    - tests/test_resources.py: TestParseHrefId.test_parse_href_id_happy_paths           (line ~38)
    - tests/test_resources.py: TestParseHrefId.test_parse_href_id_returns_none_for_invalid (line ~45)
    - tests/test_resources.py: TestHrefParsingRobustness.* (8 integration tests)        (lines ~787–840)

  TEST-03  unknown _type fallback in decode
    - tests/test_apiclient.py: TestDecode.test_decode_unknown_type_returns_generic     (line ~203)

  TEST-04  pageSize: null in paged response
    - tests/test_apiclient.py: TestGetWorkpackagesByQueryId.test_get_workpackages_by_query_id_pagesize_none (line ~713)
    - tests/test_apiclient.py: TestGetWorkpackagesByQueryId.test_get_workpackages_by_query_id_empty_collection (line ~697)

  TEST-05  unknown status= in get_workpackages
    - tests/test_apiclient.py: TestGetWorkpackagesUnknownStatus.test_get_workpackages_unknown_status_no_filter   (line ~600)
    - tests/test_apiclient.py: TestGetWorkpackagesUnknownStatus.test_get_workpackages_unknown_status_logs_debug  (line ~615)
    - tests/test_apiclient.py: TestGetWorkpackagesUnknownStatus.test_get_workpackages_open_status_regression     (line ~632)
    - tests/test_apiclient.py: TestGetWorkpackagesUnknownStatus.test_get_workpackages_uppercase_status_regression(line ~647)

  TEST-06  no API-key substring in DEBUG logs
    - tests/test_apiclient.py: TestApiKeyNotInDebugLogs.test_no_key_in_get_debug    (line ~1469)
    - tests/test_apiclient.py: TestApiKeyNotInDebugLogs.test_no_key_in_post_debug   (line ~1487)
    - tests/test_apiclient.py: TestApiKeyNotInDebugLogs.test_no_key_in_patch_debug  (line ~1505)
    - tests/test_apiclient.py: TestApiKeyNotInDebugLogs.test_no_key_in_delete_debug (line ~1523)

  TEST-07  Session reuse across two calls
    - tests/test_apiclient.py: TestSessionReuse.test_session_is_reused_across_calls (line ~168)

  TEST-08  timeout / verify propagation to requests
    - tests/test_apiclient.py: TestSecurityKwargsPropagation.test_http_get_default_propagates_none_and_true  (line ~116)
    - tests/test_apiclient.py: TestSecurityKwargsPropagation.test_http_get_custom_propagates                 (line ~124)
    - tests/test_apiclient.py: TestSecurityKwargsPropagation.test_http_post_propagates                       (line ~132)
    - tests/test_apiclient.py: TestSecurityKwargsPropagation.test_http_patch_propagates                      (line ~140)
    - tests/test_apiclient.py: TestSecurityKwargsPropagation.test_http_delete_propagates                     (line ~148)
    - tests/test_apiclient.py: TestSecurityKwargsPropagation.test_ca_bundle_path_propagates                  (line ~156)

Strong prior: all 8 rows will resolve to COVERED. Task 2 is expected to be a no-op
that records "0 gaps found" rather than writing new tests. Do not invent gaps to
fill — duplicate coverage is explicitly out of scope per the phase brief.
-->
</known_existing_coverage>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit — produce AUDIT.md traceability table for TEST-01..TEST-08</name>
  <files>.planning/phases/04-test-gap-closure/AUDIT.md</files>
  <action>
For each TEST-XX requirement (TEST-01..TEST-08) in `.planning/REQUIREMENTS.md`:

1. Re-derive the citation rather than trusting the `<known_existing_coverage>` hints blindly. For each requirement, run a targeted grep to locate the test(s):
   - TEST-01: `grep -nE "test_collection_(missing_embedded|embedded_without_elements|empty_elements_regression|items_default_is_list)" tests/test_resources.py`
   - TEST-02: `grep -nE "TestParseHrefId|TestHrefParsingRobustness" tests/test_resources.py`
   - TEST-03: `grep -nE "test_decode_unknown_type_returns_generic" tests/test_apiclient.py`
   - TEST-04: `grep -nE "test_get_workpackages_by_query_id_pagesize_none|test_get_workpackages_by_query_id_empty_collection" tests/test_apiclient.py`
   - TEST-05: `grep -nE "TestGetWorkpackagesUnknownStatus|test_get_workpackages_unknown_status" tests/test_apiclient.py`
   - TEST-06: `grep -nE "TestApiKeyNotInDebugLogs|test_no_key_in_(get|post|patch|delete)_debug" tests/test_apiclient.py`
   - TEST-07: `grep -nE "TestSessionReuse|test_session_is_reused_across_calls" tests/test_apiclient.py`
   - TEST-08: `grep -nE "TestSecurityKwargsPropagation|test_http_(get|post|patch|delete).*propagates|test_ca_bundle_path_propagates" tests/test_apiclient.py`

2. For each requirement, also confirm the test actually exercises the documented behavior by reading the test body (one Read call per file is sufficient — extract everything in one pass; do not re-read). Sanity criteria:
   - TEST-01: test inputs include a dict with no `_embedded` key AND a dict with `_embedded: {}` (no `elements`).
   - TEST-02: test inputs include `None`, empty string `""`, and a string starting with `urn:` for at least one resource class.
   - TEST-03: test passes a `_type` value that is not registered in `ApiClient`'s decoder map and asserts a `GenericType` (or equivalent fallback) is returned without raising.
   - TEST-04: test response JSON has `pageSize: null` (and/or `offset: null`) and asserts the pagination loop terminates correctly (no `TypeError`).
   - TEST-05: test passes a status string that is NOT in the known status set and asserts (a) no `filters=` URL query is sent and (b) a DEBUG log record is emitted naming the unknown status.
   - TEST-06: test sets caplog level to DEBUG, performs the http verb, and asserts the literal API key string does NOT appear anywhere in `record.getMessage()` or `str(record.args)` for any captured record.
   - TEST-07: test asserts `client._session` is a `requests.Session`, that `id(client._session)` is stable across two sequential `http_get` calls, and that `len(responses.calls) == 2`.
   - TEST-08: test asserts both `timeout=` and `verify=` (the requests library's actual kwarg names) are propagated; either by patching `requests.Session.<verb>` and inspecting `call_args.kwargs`, or by asserting on `client._session.verify` (per 03-01 migration).

3. Write `.planning/phases/04-test-gap-closure/AUDIT.md` with this exact structure (use the Write tool — NEVER heredoc):

```markdown
# Phase 04 — TEST-01..08 Coverage Audit

**Audit date:** {today}
**Auditor:** Claude executor (gsd-execute-phase, plan 04-01)
**Source of truth for gaps:** .planning/codebase/CONCERNS.md → "Test Coverage Gaps"
**Source of truth for requirements:** .planning/REQUIREMENTS.md → TEST section

## Method

For each TEST-XX requirement, grep for test functions whose name or class
matches the documented behavior, then read the test body to confirm it
exercises the requirement's actual contract (not just a related path).
A row is marked COVERED only if at least one existing test exercises the
exact behavior described in the requirement. Otherwise: GAP.

## Traceability Table

| Req | Behavior | Test function(s) | File:line | Disposition |
|-----|----------|------------------|-----------|-------------|
| TEST-01 | Collection missing `_embedded` / `_embedded.elements` iterates safely | `TestCollection.test_collection_missing_embedded`, `TestCollection.test_collection_embedded_without_elements`, `TestCollection.test_collection_empty_elements_regression`, `TestCollection.test_collection_items_default_is_list` | tests/test_resources.py:{line}, … | COVERED |
| TEST-02 | href parsing: None / empty / URN → None (not ValueError) | `TestParseHrefId.test_parse_href_id_happy_paths`, `TestParseHrefId.test_parse_href_id_returns_none_for_invalid`, plus 8× `TestHrefParsingRobustness.test_*` | tests/test_resources.py:{line}, … | COVERED |
| TEST-03 | unknown `_type` in `decode()` returns GenericType (no raise) | `TestDecode.test_decode_unknown_type_returns_generic` | tests/test_apiclient.py:{line} | COVERED |
| TEST-04 | `pageSize: null` in paged response — loop terminates | `TestGetWorkpackagesByQueryId.test_get_workpackages_by_query_id_pagesize_none`, `…_empty_collection` | tests/test_apiclient.py:{line}, … | COVERED |
| TEST-05 | unknown `status=` → silent no-op + DEBUG log | `TestGetWorkpackagesUnknownStatus.test_get_workpackages_unknown_status_no_filter`, `…_logs_debug`, `…_open_status_regression`, `…_uppercase_status_regression` | tests/test_apiclient.py:{line}, … | COVERED |
| TEST-06 | API key substring absent from DEBUG log records for GET/POST/PATCH/DELETE | `TestApiKeyNotInDebugLogs.test_no_key_in_get_debug`, `…_post_debug`, `…_patch_debug`, `…_delete_debug` | tests/test_apiclient.py:{line}, … | COVERED |
| TEST-07 | Session reused across two sequential http_get calls | `TestSessionReuse.test_session_is_reused_across_calls` | tests/test_apiclient.py:{line} | COVERED |
| TEST-08 | `timeout` and `verify` propagated to all four http_* methods | `TestSecurityKwargsPropagation.test_http_get_default_propagates_none_and_true`, `…_custom_propagates`, `…_http_post_propagates`, `…_http_patch_propagates`, `…_http_delete_propagates`, `…_ca_bundle_path_propagates` | tests/test_apiclient.py:{line}, … | COVERED |

## Gap Summary

- **Total requirements audited:** 8
- **COVERED:** {N}
- **GAP:** {8 - N}
- **Action:** {“No gap-fill work required” if N == 8; otherwise list each GAP with the new test to add in Task 2}

## Replace placeholders

Every `{line}` placeholder MUST be replaced with the actual current line number from `grep -n`. Every `{N}` placeholder MUST be replaced with the actual count. Do not commit a file with literal `{line}` or `{N}` placeholders.

## Cross-check against CONCERNS.md "Test Coverage Gaps"

| CONCERNS.md gap | Maps to TEST-XX | Now COVERED? |
|-----------------|-----------------|--------------|
| `urn:openproject-org:api:v3:undisclosed` href in WorkPackage | TEST-02 | yes (TestHrefParsingRobustness.test_workpackage_parent_undisclosed_urn) |
| `Collection` with missing `_embedded` / `_embedded.elements` | TEST-01 | yes (TestCollection.test_collection_missing_embedded, …_embedded_without_elements) |
| `get_workpackages_by_query_id` pagination (incl. pageSize null) | TEST-04 | yes (TestGetWorkpackagesByQueryId.test_get_workpackages_by_query_id_pagesize_none + …_multipage) |
| Network errors / timeouts | (out of scope — not a TEST-XX requirement; remains an open Medium concern) | n/a |
| CLI missing-mode path | (out of scope — not a TEST-XX requirement; remains an open Low concern) | n/a |
```
  </action>
  <verify>
    <automated>test -f .planning/phases/04-test-gap-closure/AUDIT.md &amp;&amp; grep -c "^| TEST-0[1-8]" .planning/phases/04-test-gap-closure/AUDIT.md | awk '$1==8 {exit 0} {exit 1}' &amp;&amp; grep -cE "tests/test_(apiclient|resources)\\.py:[0-9]+" .planning/phases/04-test-gap-closure/AUDIT.md | awk '$1>=8 {exit 0} {exit 1}' &amp;&amp; ! grep -E "\\{line\\}|\\{N\\}" .planning/phases/04-test-gap-closure/AUDIT.md</automated>
  </verify>
  <done>
AUDIT.md exists with one row per TEST-XX (8 rows), each row carries at least one `tests/test_*.py:NNN` citation pointing to a real line, every `{line}`/`{N}` placeholder has been replaced with a concrete value, and the Gap Summary states the COVERED/GAP counts. The cross-check table maps each CONCERNS.md "Test Coverage Gap" to its TEST-XX (or marks it out of scope).
  </done>
</task>

<task type="auto">
  <name>Task 2: Gap fill — add tests only for rows the audit marked GAP</name>
  <files>tests/test_apiclient.py, tests/test_resources.py</files>
  <action>
Read AUDIT.md from Task 1. Branch on the Gap Summary:

**Case A — `GAP: 0` (the expected case):**
This task is a no-op for production code. Do NOT add tests. Do NOT refactor existing tests. Do NOT widen scope. Record in the plan SUMMARY that no gap-fill work was required and that the audit found full coverage. Proceed directly to the verify step.

**Case B — `GAP: > 0`:**
For each GAP row, add a single focused test function:

1. Place the test in the file where the related existing tests already live:
   - TEST-01 → `tests/test_resources.py`, inside or adjacent to `TestCollection`.
   - TEST-02 → `tests/test_resources.py`, inside or adjacent to `TestParseHrefId` / `TestHrefParsingRobustness`.
   - TEST-03 → `tests/test_apiclient.py`, inside or adjacent to `TestDecode`.
   - TEST-04 → `tests/test_apiclient.py`, inside or adjacent to `TestGetWorkpackagesByQueryId` / `TestGetPagedCollection`.
   - TEST-05 → `tests/test_apiclient.py`, inside `TestGetWorkpackagesUnknownStatus`.
   - TEST-06 → `tests/test_apiclient.py`, inside `TestApiKeyNotInDebugLogs`.
   - TEST-07 → `tests/test_apiclient.py`, inside `TestSessionReuse`.
   - TEST-08 → `tests/test_apiclient.py`, inside `TestSecurityKwargsPropagation`.

2. Follow existing patterns verbatim:
   - HTTP mocking: `responses` library (`@responses.activate`, `responses.add(responses.GET, …)`); do NOT introduce a new mocking library.
   - Logging assertions: `caplog` fixture, `caplog.set_level(logging.DEBUG, logger="openproject_api_client.apiclient")`.
   - Fixtures: reuse `status_json`, `workpackage_json`, `project_json`, `make_collection`, `BASE_URL`, `API_KEY` from `tests/conftest.py` — do not invent new ones.
   - Session-level patches (TEST-07/TEST-08): patch `requests.Session.get` / `requests.Session.post` / … (per the 03-01 migration), not the module-level `requests.get`.

3. Name new tests with a descriptive snake_case name (e.g. `test_pagesize_null_terminates_paged_collection`). Update AUDIT.md to upgrade the GAP row's disposition to COVERED with the new file:line citation.

4. Do NOT add `pytest-cov`, `hypothesis`, `pytest-asyncio`, or any other new test dependency. Do NOT modify `pyproject.toml`. Do NOT add or remove a linter / formatter. Do NOT refactor any pre-existing test.

In both cases, after Task 2 finishes, AUDIT.md must contain zero `GAP` rows.
  </action>
  <verify>
    <automated>grep -c "| GAP " .planning/phases/04-test-gap-closure/AUDIT.md | awk '$1==0 {exit 0} {exit 1}' &amp;&amp; git diff --name-only pyproject.toml | awk 'NF==0 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
AUDIT.md has zero rows marked GAP (all 8 rows show COVERED), `pyproject.toml` is unchanged in this commit, and either no test files were modified (Case A) or only the targeted gap-fill test functions were added (Case B) without touching pre-existing tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verification — full suite green and every TEST-XX has ≥1 named test</name>
  <files>(verification only — no code changes)</files>
  <action>
Run the full test suite and confirm every TEST-XX requirement maps to at least one direct named test in the final tree:

1. `pytest -v -x` — must exit 0. Record the total passed count.
2. For each TEST-XX, confirm via grep that the citation in AUDIT.md still resolves on the current tree (catches the case where Task 2 accidentally renamed something):
   ```
   grep -nE "test_collection_missing_embedded|test_collection_embedded_without_elements" tests/test_resources.py     # TEST-01
   grep -nE "TestParseHrefId|TestHrefParsingRobustness"                                  tests/test_resources.py     # TEST-02
   grep -nE "test_decode_unknown_type_returns_generic"                                   tests/test_apiclient.py     # TEST-03
   grep -nE "test_get_workpackages_by_query_id_pagesize_none"                            tests/test_apiclient.py     # TEST-04
   grep -nE "TestGetWorkpackagesUnknownStatus"                                           tests/test_apiclient.py     # TEST-05
   grep -nE "TestApiKeyNotInDebugLogs"                                                   tests/test_apiclient.py     # TEST-06
   grep -nE "TestSessionReuse"                                                           tests/test_apiclient.py     # TEST-07
   grep -nE "TestSecurityKwargsPropagation"                                              tests/test_apiclient.py     # TEST-08
   ```
   Every grep must return at least one match.
3. Confirm runtime deps unchanged: `git diff pyproject.toml` is empty.
4. Record the final pytest summary line (`N passed in T s`) and the resolved citation set in the plan SUMMARY's verification section.
  </action>
  <verify>
    <automated>cd /usr/src/devpad/mhx-github/openproject-api-client &amp;&amp; pytest -v -x &amp;&amp; grep -qE "test_collection_missing_embedded" tests/test_resources.py &amp;&amp; grep -qE "TestParseHrefId" tests/test_resources.py &amp;&amp; grep -qE "test_decode_unknown_type_returns_generic" tests/test_apiclient.py &amp;&amp; grep -qE "test_get_workpackages_by_query_id_pagesize_none" tests/test_apiclient.py &amp;&amp; grep -qE "TestGetWorkpackagesUnknownStatus" tests/test_apiclient.py &amp;&amp; grep -qE "TestApiKeyNotInDebugLogs" tests/test_apiclient.py &amp;&amp; grep -qE "TestSessionReuse" tests/test_apiclient.py &amp;&amp; grep -qE "TestSecurityKwargsPropagation" tests/test_apiclient.py &amp;&amp; [ -z "$(git diff --name-only pyproject.toml)" ]</automated>
  </verify>
  <done>
`pytest -v -x` exits 0; every TEST-XX has at least one grep-resolvable named test; `pyproject.toml` is unchanged; the SUMMARY records the final pytest count and the resolved citation set.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds no new trust boundaries. The only artifact written outside of tests is `AUDIT.md`, a planning document under `.planning/` — not loaded at runtime by the library or CLI.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information disclosure | `tests/test_apiclient.py` (TestApiKeyNotInDebugLogs reused / extended) | mitigate | Continue using a distinctive `SECRET_KEY` constant (e.g. `"SUPERSECRETKEY-XYZ-12345"`) in any new TEST-06 gap-fill test so a leak is visually unambiguous; never use the production-shaped key `"test-api-key"` alone as the only assertion target. |
| T-04-02 | Tampering | `.planning/phases/04-test-gap-closure/AUDIT.md` | accept | The audit is a developer-facing planning artifact, not runtime input. Tampering risk is equivalent to tampering with any other doc in `.planning/`. No mitigation beyond normal git history. |
| T-04-03 | Repudiation | Audit traceability (TEST-XX → test function citations) | mitigate | AUDIT.md cites file:line for every COVERED row so future readers can verify the claim against the current tree without re-deriving the audit. Task 3 grep-recheck catches stale citations. |
</threat_model>

<verification>
Phase-level checks:

1. `.planning/phases/04-test-gap-closure/AUDIT.md` exists and has 8 TEST-XX rows, all COVERED, all with `tests/test_*.py:NNN` citations resolvable on the current tree.
2. `pytest -v -x` exits 0.
3. `git diff pyproject.toml` is empty (no new test or coverage dependencies introduced).
4. No test file was refactored — only net-additive changes (and only if Task 2's Case B triggered). Confirm with `git diff --stat tests/` — the lines-removed count should be 0 unless a gap-fill test legitimately required a fixture tweak.
</verification>

<success_criteria>
- AUDIT.md is committed and lists every TEST-XX → named test → file:line.
- Every TEST-XX has ≥1 named test function on the current tree (greppable).
- `pytest -v -x` exits 0.
- `pyproject.toml` runtime dependencies unchanged.
- No new linter / formatter / coverage tool introduced.
- No pre-existing test refactored.
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-gap-closure/04-01-SUMMARY.md` describing:
- The final disposition of each TEST-XX row (COVERED + which test function(s)).
- Whether Task 2 was a no-op (expected) or added gap-fill tests (and which).
- The final `pytest -v -x` summary line.
- Confirmation that `pyproject.toml` is untouched.
</output>
