---
phase: 02-security-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - openproject_api_client/apiclient.py
  - tests/test_apiclient.py
autonomous: true
requirements:
  - SEC-03
  - SEC-04
must_haves:
  truths:
    - "The API key is stored on the private attribute `self._apikey` (not directly assigned to `self.apikey`)."
    - "`client.apikey` still returns the API key value for backwards compatibility (via a @property reading `self._apikey`)."
    - "`repr(client)` does not contain the API key value."
    - "DEBUG-level log output for representative GET, POST, PATCH, DELETE calls contains zero substrings of the API key."
    - "pytest -v -x exits 0; existing TestApiClientInit.test_valid_construction still passes (it reads .apikey)."
  artifacts:
    - path: openproject_api_client/apiclient.py
      provides: "Private _apikey storage; apikey @property accessor; __repr__ override that masks the key."
      contains: "@property"
    - path: tests/test_apiclient.py
      provides: "Regression tests for backwards-compat .apikey accessor, __repr__ masking, and caplog regression test for no-key-in-DEBUG."
      contains: "TestApiKeyPrivacy"
  key_links:
    - from: "ApiClient.__init__"
      to: "self._apikey storage"
      via: "private attribute"
      pattern: "self\\._apikey\\s*="
    - from: "ApiClient.apikey property"
      to: "self._apikey"
      via: "@property getter"
      pattern: "@property"
    - from: "caplog DEBUG records"
      to: "no API-key substring"
      via: "assertion over caplog.records at DEBUG level"
      pattern: "caplog.at_level\\(logging.DEBUG"
---

<objective>
Make the API key non-public on `ApiClient` while keeping the legacy `client.apikey` read accessor working. Lock in (via a regression test) that DEBUG-level logging in `apiclient.py` never leaks any substring of the API key.

Purpose: SEC-03 and SEC-04 — close the key-exposure surface (attribute access, accidental repr/log) without breaking the v0.2.x public API.
Output: Updated `apiclient.py` with `self._apikey` storage, an `apikey` `@property`, and a masking `__repr__`; new test class covering backwards-compat, repr masking, and DEBUG-log non-leakage on representative GET / POST / PATCH / DELETE calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@.planning/phases/02-security-baseline/02-01-SUMMARY.md
@openproject_api_client/apiclient.py
@tests/test_apiclient.py
@tests/conftest.py

<interfaces>
<!-- Current state (line 45 of apiclient.py): -->
<!--   self.apikey = apikey -->
<!--   self.auth = HTTPBasicAuth('apikey', apikey) -->
<!-- After this plan: -->
<!--   self._apikey = apikey -->
<!--   self.auth = HTTPBasicAuth('apikey', self._apikey) -->
<!--   @property -->
<!--   def apikey(self): return self._apikey -->
<!--   def __repr__(self): return f"ApiClient(base_url={self.base_url!r}, apikey='***')" -->
<!-- Logger name for caplog regression: "openproject_api_client.apiclient" -->
<!-- Existing DEBUG log lines (apiclient.py lines 69, 76, 82, 89, 95, 102, 108, 114, 176, 194, 288, 322) — none currently format the API key. The regression test locks that in. -->
<!-- Test fixture: API_KEY = "test-api-key" (defined at top of tests/test_apiclient.py line 32). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename to self._apikey, add apikey @property, add masking __repr__</name>
  <read_first>
    - openproject_api_client/apiclient.py (lines 35-50 — the __init__ block from 02-01)
    - tests/test_apiclient.py (lines 37-54 — TestApiClientInit, must continue to pass since it reads `c.apikey`)
    - .planning/REQUIREMENTS.md (SEC-03 exact wording, including the "does not appear in repr()/vars() output" qualifier)
    - .planning/codebase/CONVENTIONS.md (Naming section — leading-underscore convention)
  </read_first>
  <behavior>
    - After `c = ApiClient(BASE_URL, API_KEY)`: `c._apikey == API_KEY` (private storage).
    - `'apikey' not in vars(c)` — the public `apikey` name is NOT in the instance `__dict__` (it lives on the class as a @property), locking SEC-03 in against future refactors that might reintroduce `self.apikey = ...`.
    - `c.apikey == API_KEY` (backwards-compatible read via property).
    - `ApiClient.apikey` is a `property` object (`isinstance(ApiClient.__dict__['apikey'], property) is True`).
    - The property has no setter — `c.apikey = "x"` raises `AttributeError`.
    - `repr(c)` does NOT contain `API_KEY` as a substring; the masked form (e.g. `***`) appears instead.
    - `repr(c)` still contains the base_url (useful for debugging).
    - `c.auth` continues to be an `HTTPBasicAuth` object constructed with the API key (authentication remains unaffected).
    - Existing tests `test_valid_construction`, `test_trailing_slash_added`, `test_missing_base_url_raises`, `test_missing_apikey_raises` all still pass unchanged.
  </behavior>
  <action>
    In `openproject_api_client/apiclient.py`:
    1. In `__init__` (around line 45), replace the line `self.apikey = apikey` with `self._apikey = apikey`. Update the next line so `HTTPBasicAuth` is constructed from `self._apikey` (or the local `apikey` parameter — equivalent). Keep the existing ApiError validation and the timeout/verify_ssl assignments from 02-01 unchanged.
    2. Add a `@property` named `apikey` on `ApiClient` that returns `self._apikey`. Do NOT define a setter — backwards compatibility is read-only by design. Place the property near the top of the class, after `__init__` and before `_endpoint`, so it is grouped with other lightweight accessors.
    3. Add a `__repr__` method on `ApiClient` that returns a string of the form `ApiClient(base_url=<base_url>, apikey='***')`. The literal `***` (or any other placeholder containing no characters of the real key) MUST be used; do NOT interpolate `self._apikey` into the repr in any form. Use the positional `%s`-style or an f-string with a literal `'***'` — either is fine, but the API key value must never reach the returned string.

    In `tests/test_apiclient.py`, add a new test class `TestApiKeyPrivacy` placed after `TestSecurityKwargsPropagation` (from 02-01 Task 2). Cover every bullet from `<behavior>`:
    - `test_private_storage` — asserts `c._apikey == API_KEY`.
    - `test_backwards_compat_property` — asserts `c.apikey == API_KEY` and `isinstance(type(c).__dict__['apikey'], property)`.
    - `test_property_is_readonly` — uses `pytest.raises(AttributeError)` around `c.apikey = "new"`.
    - `test_repr_masks_apikey` — asserts `API_KEY not in repr(c)` and `'***' in repr(c)` and `BASE_URL in repr(c)`.
    - `test_auth_object_still_carries_key` — asserts `c.auth.password == API_KEY` (HTTPBasicAuth stores the key on `.password`; this is the legitimate transport location for it per CONCERNS.md "fix approach").
  </action>
  <verify>
    <automated>pytest tests/test_apiclient.py::TestApiKeyPrivacy tests/test_apiclient.py::TestApiClientInit -v -x</automated>
  </verify>
  <acceptance_criteria>
    - All `TestApiKeyPrivacy` tests pass.
    - All four `TestApiClientInit` tests still pass — `test_valid_construction` (which asserts `c.apikey == API_KEY`) is the regression gate for backwards compatibility.
    - `grep -n "self._apikey" openproject_api_client/apiclient.py` shows the assignment in `__init__` and the property body.
    - `grep -c "self\.apikey\s*=" openproject_api_client/apiclient.py | tr -d '[:space:]'` — direct assignment to `self.apikey` (with `=`) is gone (count = 0 outside of test files; the property declaration uses `def apikey(self):` which does not match this pattern).
    - `python -c "from openproject_api_client.apiclient import ApiClient; c=ApiClient('https://x/','SECRET123'); assert 'SECRET123' not in repr(c) and '***' in repr(c)"` exits 0.
    - `grep -F "'apikey' not in vars(" tests/test_apiclient.py` returns at least one match (the structural assertion is locked into the test file).
  </acceptance_criteria>
  <done>API key lives on `self._apikey`; `c.apikey` returns it read-only via `@property`; `repr(c)` masks the key; existing init tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Caplog regression test — no API-key substring in DEBUG output for GET / POST / PATCH / DELETE</name>
  <read_first>
    - openproject_api_client/apiclient.py (the four http_* methods at lines 65-115 — verify the logger.debug lines do NOT format the API key. They currently log only endpoint and status_code.)
    - tests/test_apiclient.py (lines 475-490 — TestGetWorkpackagesUnknownStatus.test_get_workpackages_unknown_status_logs_debug — the canonical caplog pattern for this codebase: `caplog.at_level(logging.DEBUG, logger="openproject_api_client.apiclient")`)
    - .planning/codebase/CONVENTIONS.md (Logging section — positional %s format)
    - .planning/codebase/TESTING.md (caplog usage notes if present)
  </read_first>
  <behavior>
    - Using a distinctive test API key (e.g. `"SUPERSECRETKEY-XYZ-12345"`) and triggering one GET, one POST, one PATCH, and one DELETE via `responses`-mocked endpoints, NO `caplog.records` entry at any level contains the API-key substring.
    - Specifically, the assertion is per-request: for each of the four representative calls, `all("SUPERSECRETKEY-XYZ-12345" not in r.getMessage() for r in caplog.records)` holds.
    - Defensive belt-and-suspenders: each of the four caplog tests ALSO asserts that the fixture-level default `"test-api-key"` (the value of `API_KEY` at tests/test_apiclient.py:32) does not appear in any captured record message — catches the latent risk that a fixture happens to embed that string in JSON payloads or headers logging.
    - The test also checks that *some* DEBUG records were emitted (sanity check that the caplog filter is wired correctly — otherwise the test could pass vacuously). At minimum one DEBUG record per call containing the endpoint path.
    - Negative-control sanity: explicitly assert the rendered message string of the DEBUG record does not contain the key, in addition to the raw `.message` field, since callers sometimes confuse the two.
  </behavior>
  <action>
    In `tests/test_apiclient.py`, add a new test class `TestApiKeyNotInDebugLogs` immediately after `TestApiKeyPrivacy`. Define a class-level constant `SECRET_KEY = "SUPERSECRETKEY-XYZ-12345"` (distinctive enough that any substring leak is obvious and unambiguous).

    Write four `@responses.activate`-decorated tests:
    - `test_no_key_in_get_debug` — registers a GET on `f"{BASE_URL}api/v3/statuses/1"` returning `status_json`, constructs `ApiClient(BASE_URL, SECRET_KEY)`, enters `caplog.at_level(logging.DEBUG, logger="openproject_api_client.apiclient")`, calls `client.http_get("statuses/1")`. Assert at least one DEBUG record was captured, and for every record `r` in `caplog.records`, both `SECRET_KEY not in r.getMessage()` AND `SECRET_KEY not in str(r.args)` (covers positional %s args that have not yet been rendered). Additionally, for every record `r`, assert `"test-api-key" not in r.getMessage()  # belt + suspenders` — defensive parallel check guarding against fixture-string leaks. Apply the same defensive line to all four caplog tests (GET, POST, PATCH, DELETE).
    - `test_no_key_in_post_debug` — same pattern for POST against `projects/1/work_packages` returning a workpackage_json fixture.
    - `test_no_key_in_patch_debug` — same pattern for PATCH against `work_packages/42`.
    - `test_no_key_in_delete_debug` — same pattern for DELETE against `work_packages/42` (status 204; no JSON body needed).

    Each test must use `caplog.clear()` at the start or rely on pytest's per-test caplog reset. Use the `status_json`, `workpackage_json` fixtures from `tests/conftest.py` where applicable.

    Do not modify `apiclient.py` in this task. If a test fails (i.e. the key DOES appear in a DEBUG log), that is a real SEC-04 bug — stop and fix the offending logger.debug line by removing the API-key reference. The current DEBUG calls in the codebase do not log the key, so the tests should pass without code changes; this task locks that invariant in.
  </action>
  <verify>
    <automated>pytest tests/test_apiclient.py::TestApiKeyNotInDebugLogs -v -x</automated>
  </verify>
  <acceptance_criteria>
    - All four `TestApiKeyNotInDebugLogs` tests pass.
    - Each test captures at least one DEBUG-level record from logger `openproject_api_client.apiclient` (so the assertion is not vacuous).
    - For every captured record, `SECRET_KEY` does not appear in `record.getMessage()` nor in `str(record.args)`.
    - For every captured record, `"test-api-key"` (fixture default) also does not appear in `record.getMessage()` — defensive parallel check is wired in each of the four caplog tests.
    - `grep -c 'test-api-key" not in' tests/test_apiclient.py` is at least 4 (one defensive assertion per caplog test).
    - Full `pytest -v -x` exits 0.
    - `grep -n "self._apikey\|self.apikey\|apikey" openproject_api_client/apiclient.py | grep -i "logger\." | wc -l` is 0 (no logger statement references the apikey attribute by any name).
  </acceptance_criteria>
  <done>Regression test asserts the API key never appears in DEBUG output for representative GET / POST / PATCH / DELETE calls; SEC-04 is locked in by automated test, not just by inspection.</done>
</task>

</tasks>

<verification>
- `pytest -v -x` exits 0.
- `ApiClient("https://x/", "k").apikey == "k"` (backwards-compat read accessor preserved).
- `ApiClient.apikey` is a `property` and has no setter.
- `repr(ApiClient("https://x/", "k"))` does not contain `"k"` and contains `"***"`.
- For a distinctive API key, no `caplog.records` entry from the `openproject_api_client.apiclient` logger contains the key substring across GET / POST / PATCH / DELETE.
- `git diff` does NOT remove the `apikey` accessor (the `@property` keeps the read path working).
- No new runtime dependency added to `pyproject.toml`.
</verification>

<success_criteria>
SEC-03 satisfied: API key stored on private `self._apikey`; public `self.apikey` accessor remains for backwards compatibility (read-only); `repr()` does not leak the key.
SEC-04 satisfied: regression test asserts no API-key substring appears in DEBUG log output for representative GET / POST / PATCH / DELETE calls.
Non-breaking: existing positional `ApiClient(base_url, apikey)` callers and `client.apikey` reads continue to work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-baseline/02-02-SUMMARY.md`.
</output>
