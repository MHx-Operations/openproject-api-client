---
phase: 02-security-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openproject_api_client/apiclient.py
  - tests/test_apiclient.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "ApiClient(base_url, apikey) keeps working unchanged (no breaking change)."
    - "ApiClient accepts keyword-only timeout and verify_ssl kwargs; defaults preserve current behavior (timeout=None, verify_ssl=True)."
    - "Every requests.get/post/patch/delete call inside the four http_* methods receives timeout=self._timeout and verify=self._verify_ssl."
    - "Tests assert kwargs are propagated for at least one GET and one POST/PATCH/DELETE."
    - "pytest -v -x exits 0."
  artifacts:
    - path: openproject_api_client/apiclient.py
      provides: "ApiClient.__init__ extended with timeout / verify_ssl kwargs; http_get, http_post, http_patch, http_delete forward both to requests."
      contains: "self._timeout"
    - path: tests/test_apiclient.py
      provides: "Regression tests for timeout/verify_ssl propagation and default-preservation."
      contains: "TestApiClientSecurityKwargs"
  key_links:
    - from: "ApiClient.__init__"
      to: "self._timeout / self._verify_ssl attributes"
      via: "kwargs storage"
      pattern: "self\\._timeout|self\\._verify_ssl"
    - from: "http_get / http_post / http_patch / http_delete"
      to: "requests.get / post / patch / delete"
      via: "timeout=self._timeout, verify=self._verify_ssl"
      pattern: "timeout=self\\._timeout"
---

<objective>
Add opt-in `timeout` and `verify_ssl` kwargs to `ApiClient.__init__` and propagate them to all four HTTP methods. Defaults preserve current behavior so the change is non-breaking.

Purpose: SEC-01 and SEC-02 — eliminate two security/operational footguns (indefinite hang, no SSL override) by making safe-by-default-opt-in possible without breaking the v0.2.x positional constructor.
Output: Updated `apiclient.py` with new kwargs threaded to all `requests.*` calls; new test class asserting propagation + default behavior preservation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-latent-bug-fixes/01-03-SUMMARY.md
@openproject_api_client/apiclient.py
@tests/test_apiclient.py
@tests/conftest.py

<interfaces>
<!-- Current ApiClient.__init__ signature (apiclient.py:35): -->
<!--   def __init__(self, base_url, apikey): -->
<!-- Stored attributes: self._rootpath, self.base_url, self.apikey, self.auth -->
<!-- The four HTTP methods (apiclient.py:65-115) currently call: -->
<!--   requests.get(endpoint, params=payload, headers=..., auth=self.auth) -->
<!--   requests.post(endpoint, json=body, headers=..., auth=self.auth) -->
<!--   requests.patch(endpoint, json=body, headers=..., auth=self.auth) -->
<!--   requests.delete(endpoint, headers=..., auth=self.auth) -->
<!-- New signature shape (keyword-only, after positional args, matches CONVENTIONS.md write-method pattern): -->
<!--   def __init__(self, base_url, apikey, *, timeout: float | None = None, verify_ssl: bool | str = True): -->
<!-- Test conventions (CONVENTIONS.md + existing tests): -->
<!--   - responses library mocks the HTTP layer (TestHttpGet, TestHttpPost) -->
<!--   - To inspect timeout/verify kwargs, prefer unittest.mock.patch on requests.get/post/etc. -->
<!--     because the `responses` library intercepts at a lower layer and may not surface the timeout kwarg. -->
<!--   - Logger name for caplog: "openproject_api_client.apiclient" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ApiClient.__init__ with keyword-only timeout and verify_ssl; store on private attrs</name>
  <read_first>
    - openproject_api_client/apiclient.py (lines 25-50 for current __init__ shape; do not re-read once cached)
    - .planning/codebase/CONVENTIONS.md (write-method keyword-only pattern at lines 53-66; logging at lines 76-79)
    - .planning/REQUIREMENTS.md (SEC-01, SEC-02 exact wording)
  </read_first>
  <behavior>
    - `ApiClient(BASE_URL, API_KEY)` (positional only) still constructs successfully and `.apikey == API_KEY` (regression — must not break).
    - `ApiClient(BASE_URL, API_KEY).` default attributes: `c._timeout is None`, `c._verify_ssl is True`.
    - `ApiClient(BASE_URL, API_KEY, timeout=30)` sets `c._timeout == 30`.
    - `ApiClient(BASE_URL, API_KEY, verify_ssl=False)` sets `c._verify_ssl is False`.
    - `ApiClient(BASE_URL, API_KEY, verify_ssl="/path/to/ca.pem")` sets `c._verify_ssl == "/path/to/ca.pem"` (CA-bundle path form).
    - Passing `timeout` or `verify_ssl` positionally raises `TypeError` (keyword-only enforced via `*`).
    - `inspect.signature(ApiClient.__init__)` shows both new parameters as KEYWORD_ONLY with defaults `None` and `True` respectively.
  </behavior>
  <action>
    Modify `ApiClient.__init__` in `openproject_api_client/apiclient.py` (around line 35) to add a `*` separator followed by two keyword-only parameters: `timeout: float | None = None` and `verify_ssl: bool | str = True`. Match the keyword-only-after-positional pattern documented at CONVENTIONS.md lines 53-66 (the same pattern `create_workpackage` uses). Store as `self._timeout = timeout` and `self._verify_ssl = verify_ssl` (leading-underscore = module-private per CONVENTIONS.md "Naming" section). Place the assignments after the existing `self.apikey = apikey` / `self.auth = HTTPBasicAuth(...)` block (before the trailing-slash normalization at line 48 is fine; keep the existing ApiError validation for `base_url` / `apikey` exactly as-is). Do NOT add any default value for `timeout` other than `None` and do NOT touch the `self.apikey` attribute in this task — SEC-03 owns that change in 02-02.

    Add a new test class `TestApiClientSecurityKwargs` near the existing `TestApiClientInit` block in `tests/test_apiclient.py` covering each bullet from `<behavior>`. Use `inspect.signature(ApiClient.__init__).parameters` to assert KEYWORD_ONLY kind for `timeout` and `verify_ssl`. Use `pytest.raises(TypeError)` for the positional-rejection test. Do not stub out `requests` here — propagation is verified in Task 2.
  </action>
  <verify>
    <automated>pytest tests/test_apiclient.py::TestApiClientSecurityKwargs -v -x</automated>
  </verify>
  <acceptance_criteria>
    - All tests in `TestApiClientSecurityKwargs` pass.
    - `TestApiClientInit` (existing tests at line 37) still passes — positional `ApiClient(BASE_URL, API_KEY)` continues to work.
    - `grep -n "self._timeout" openproject_api_client/apiclient.py` returns at least one line in `__init__`.
    - `grep -n "self._verify_ssl" openproject_api_client/apiclient.py` returns at least one line in `__init__`.
    - `python -c "import inspect; from openproject_api_client.apiclient import ApiClient; p=inspect.signature(ApiClient.__init__).parameters; assert p['timeout'].kind.name=='KEYWORD_ONLY' and p['verify_ssl'].kind.name=='KEYWORD_ONLY' and p['timeout'].default is None and p['verify_ssl'].default is True"` exits 0.
  </acceptance_criteria>
  <done>Constructor accepts both new keyword-only kwargs with the documented defaults; private attrs are stored; existing positional construction is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Forward timeout and verify to all four http_* methods + propagation tests</name>
  <read_first>
    - openproject_api_client/apiclient.py (lines 65-115 for the four http_* methods; already in context from Task 1)
    - tests/test_apiclient.py (lines 128-160 for existing TestHttpGet shape; lines 696-755 for TestHttpPost / TestHttpPatch)
    - .planning/codebase/CONVENTIONS.md (logging section — keep positional %s format, do not introduce f-strings)
  </read_first>
  <behavior>
    - `http_get` passes `timeout=self._timeout, verify=self._verify_ssl` to `requests.get`.
    - `http_post` passes `timeout=self._timeout, verify=self._verify_ssl` to `requests.post`.
    - `http_patch` passes `timeout=self._timeout, verify=self._verify_ssl` to `requests.patch`.
    - `http_delete` passes `timeout=self._timeout, verify=self._verify_ssl` to `requests.delete`.
    - Default-behavior regression: with no kwargs supplied, the requests calls receive `timeout=None, verify=True` (current effective behavior preserved).
    - Explicit-kwargs propagation: constructing `ApiClient(..., timeout=15, verify_ssl=False)` causes every requests.* call to be invoked with `timeout=15, verify=False`.
    - All existing tests in `tests/test_apiclient.py` keep passing (no regressions in `TestHttpGet`, `TestHttpPost`, `TestHttpPatch`, `TestHttpDelete`, `TestGet`, `TestGetPagedCollection`, `TestEndpointMethods`, `TestCreateWorkpackage`, etc.).
  </behavior>
  <action>
    In `openproject_api_client/apiclient.py`, add `timeout=self._timeout, verify=self._verify_ssl` to the kwargs of each of the four `requests.*` calls in `http_get` (line 70), `http_post` (line 83), `http_patch` (line 96), `http_delete` (line 109). Keep the existing `headers`, `auth`, `params`/`json` kwargs and their formatting. Do not change the DEBUG logging statements at lines 69/76/82/89/95/102/108/114 — they already use positional %s and never reference the API key, so they remain SEC-04 safe.

    Extend `tests/test_apiclient.py` with a new test class `TestSecurityKwargsPropagation` placed after `TestApiClientSecurityKwargs` from Task 1. Use `unittest.mock.patch` on `openproject_api_client.apiclient.requests.get` (and `.post`, `.patch`, `.delete`) — not the `responses` library — because we need to inspect the exact keyword arguments passed to the underlying call. The mock should return a `MagicMock` with `status_code=200` so the existing debug logging path does not blow up. Write at minimum:
    - `test_http_get_default_propagates_none_and_true` — constructs `ApiClient(BASE_URL, API_KEY)`, calls `http_get("foo")`, asserts the patched `requests.get` was called with `timeout=None, verify=True` in its kwargs.
    - `test_http_get_custom_propagates` — constructs `ApiClient(BASE_URL, API_KEY, timeout=15, verify_ssl=False)`, calls `http_get("foo")`, asserts kwargs include `timeout=15, verify=False`.
    - One propagation test for each of `http_post`, `http_patch`, `http_delete` (parameterize or one method per test — use whichever stays under ~30 lines total).
    - A CA-bundle-path variant: `ApiClient(..., verify_ssl="/etc/ssl/ca.pem")` results in `verify="/etc/ssl/ca.pem"` on at least one method call.

    Do not modify any other method or any other test class.
  </action>
  <verify>
    <automated>pytest tests/test_apiclient.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - All four propagation tests pass.
    - Full `pytest -v -x` exits 0 — no existing test regresses.
    - `grep -v '^[[:space:]]*#' openproject_api_client/apiclient.py | grep -c 'timeout=self._timeout'` is at least 4 (one per http_* method).
    - `grep -v '^[[:space:]]*#' openproject_api_client/apiclient.py | grep -c 'verify=self._verify_ssl'` is at least 4.
    - No new entries in `pyproject.toml` dependencies (`git diff pyproject.toml` is empty for runtime deps).
  </acceptance_criteria>
  <done>All four HTTP methods forward `timeout` and `verify`; defaults preserve current behavior; explicit kwargs reach `requests.*`; full test suite green.</done>
</task>

</tasks>

<verification>
- `pytest -v -x` exits 0.
- `ApiClient("https://op.example.com/", "k")` (positional only, no new kwargs) still constructs and `.apikey == "k"`.
- `inspect.signature(ApiClient.__init__)` exposes `timeout` and `verify_ssl` as KEYWORD_ONLY with defaults `None` and `True`.
- Each `requests.{get,post,patch,delete}` call site in `apiclient.py` carries both `timeout=self._timeout` and `verify=self._verify_ssl`.
- No new runtime dependency added to `pyproject.toml`.
</verification>

<success_criteria>
SEC-01 satisfied: optional `timeout` kwarg accepted; default `None` preserves current behavior; passed to every requests call.
SEC-02 satisfied: optional `verify_ssl` kwarg accepted (bool or CA-bundle path); default `True` preserves current behavior; passed to every requests call.
Non-breaking: existing `ApiClient(base_url, apikey)` callers and `self.apikey` reads unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-baseline/02-01-SUMMARY.md`.
</output>
